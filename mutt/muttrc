set folder = "imaps://syedahmed@mail.riseup.net:993"
set smtp_url = "smtp://syedahmed@mail.riseup.net:587"

set from = "syedahmed@riseup.net"
set realname = "Syed Ahmed"

source "gpg -d ~/.config/mutt/passwords.gpg |"

set spoolfile = "+INBOX"
set record = "+Sent"

set editor = "nvim"

mailboxes =INBOX =Sent 

